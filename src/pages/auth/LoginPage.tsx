import LoginForm from "@/components/auth/LoginForm";

type LoginPageProps = {};

export default function LoginPage({}: LoginPageProps) {
  return (
    <section>
      <LoginForm />
    </section>
  );
}
